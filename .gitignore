# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev.log*

# env files (NEVER commit secrets — see CONNECTING.md)
.env
.env*.local
.env.local
.env.development.local
.env.production.local

# server-side runtime secrets (UI-managed API keys) — NEVER commit
secrets.local.json

# server-side agent persona/human version history — NEVER commit
agent-versions.local.json

# typescript
*.tsbuildinfo

# smoke-test screenshots
demo-*.png

# editor
.vscode/*
!.vscode/extensions.json
.idea

# AI description files
*.md