import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint is intentionally not a dependency in this demo (Next 16 has no built-in lint step).
};

export default nextConfig;
